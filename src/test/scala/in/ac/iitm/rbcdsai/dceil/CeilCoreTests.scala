package in.ac.iitm.rbcdsai.dceil

import org.scalatest.FunSuite

/** CeilCoreTests tests methods of CeilCore.*/
class CeilCoreTests extends FunSuite {

  // test createCeilGraph
  test("createCeilGraph should return the graph specific for CEIL") {pending}

  // test ceil
  test("ceil should return the modularity and the graph") {pending}
 
  // test sendMsg
  test("testMsg should return an iterator of two messages") {pending}

  // test mergeMsg
  test("mergeMsg should merge neighborhood community data") {pending}

  // test ceilVertJoin
  test("ceilVertJoin should make sane decisions regarding vertex movement") {pending}

  // test removal
  test("removal should return the delta Q") {pending}

  // test communityScores
  test("communityScores should return the old and the new community score") {pending}

  // test compressGraph
  test("compressGraph should return the compressed graph") {pending}
}
