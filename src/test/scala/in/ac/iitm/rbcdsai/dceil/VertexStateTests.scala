package in.ac.iitm.rbcdsai.dceil

import org.scalatest.FunSuite

/** VertexStateTests tests methods of VertexState.*/
class VertexStateTests extends FunSuite {
  // test toString()
  test ("toString should return the customized representation") {
    pending
  //   val vertexState = new VertexState

  //   assert(vertexState == "{ community: -1, communitySigmaTotal: 0, internalWeight: 0, nodeWeight: 0, internalVertices: 1, communityVertices: 1, communityInternalWeight: 0 }")
  }
}
